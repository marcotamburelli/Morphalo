include "../common.conf"
include "../characters/eurelio-base.conf"
include "../styles/cinematic.conf"

prompt = [
  ${character.base_prompt}
  """wearing a dark embroidered mage robe,
  ancient library background,
  floating arcane symbols."""
  ${style.cinematic}
]

negative_prompt = ${defaults.negative_prompt}

params {
  cfg = 5.5
}

seed = random
